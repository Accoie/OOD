<UserControl x:Class="ShapesApplication.Views.DocumentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Focusable="True"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">

    <Grid Background="Transparent" x:Name="MainGrid">
        <Border BorderBrush="Gray" BorderThickness="1" Background="White"
                Margin="10"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                ClipToBounds="True">

            <Grid>
                <Image x:Name="RenderImage" 
                       Stretch="Fill"
                       IsHitTestVisible="false"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch"/>

                <Image x:Name="SelectionRenderImage" 
                       Stretch="Fill"
                       IsHitTestVisible="false"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch"/>

                <Canvas x:Name="MainCanvas" 
                        Background="Transparent"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        MouseDown="CanvasOnMouseDown"
                        MouseMove="CanvasOnMouseMove"
                        MouseUp="CanvasOnMouseUp"
                        Focusable="True"
                        ClipToBounds="True"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>